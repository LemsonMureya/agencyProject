{
    "phases": {
      "install": "pip install -r requirements.txt",
      "build": "python manage.py collectstatic --noinput",
      "start": "python manage.py migrate && gunicorn Aximo.wsgi:application --bind 0.0.0.0:$PORT"
    }
  }